import { Routes } from '@angular/router';

export const appRoutes: Routes = [
  // Add routes here for branches and accounts
  // Example:
  // { path: 'branches', component: BranchListComponent },
  // { path: 'accounts', component: BankAccountListComponent }
];
